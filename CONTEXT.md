# loopi

issue 駆動の自動実装パイプライン。issue 本文を唯一の作業指示として、実装・レビュー・PR・自動マージまでを行う。

## Language

**受け入れ条件**:
issue に箇条書きで書かれた、実装が満たすべき条件。最終レビューはこれを 1 つずつ判定する。
_Avoid_: 完了条件、要件

**検証方法**:
受け入れ条件をどう確かめるか。「テスト」（該当テストの結果で判定する）か「レビュー確認」（レビュー担当が差分と文書を読んで判定する）のどちらかで、issue を書く人間が条件ごとに宣言する。
_Avoid_: 確認方法、検証手段

**判定結果**:
検証方法に従って条件を確かめた結果。`passed` / `failed` / `missing` の 3 値。
_Avoid_: 結果、ステータス

**根拠**:
判定結果の裏付け。テストならテスト名、レビュー確認ならファイルと行、または文書の節。
_Avoid_: 証拠、エビデンス

**モデル指定**:
実装担当・一次レビュー・取捨選択と最終レビューの各ツールに与えるモデルと effort の組。`loopi.config.json` の `models` に書く。
_Avoid_: モデル設定、モデル構成

**プロファイル**:
モデル指定の一部を上書きする組み合わせに付けた名前。実行時に `--profile` で 1 つ選ぶ。
_Avoid_: プリセット、モード
